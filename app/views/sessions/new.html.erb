<%if flash[:alert]%>
<p id="notice"><%= flash[:alert] %></p>
<%end%>
<h2 class="text-center mt-4">Login</h2>

<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>
<% if session[:user_id] %>
<%  @user = User.find_by(id: session[:user_id]) %>
    <h1>Ya estas logueado</h1>
    <h2>Usuario: <strong><%= @user.name %></strong>, tus privilegios son del tipo <strong>"<%= @user.tipo %>"</strong></h2> <%= button_to 'Logout', logout_path, method: :delete, class:"btn btn-danger btn-lg btn-block" %>
<% else %>
<div class="row mt-4">
  <div class="col-12 col-md-2">
  </div>
    <div class="col-12 col-md-8">

  <%= form_tag ({:class => 'form-inline'}) do %>

      <div class="form-group mx-sm-3 mb-2">
        <%= label_tag :name, 'Nombre, id', class:"" %>
        <%= text_field_tag :name, params[:name], class:"form-control" %>
      </div>
      <div class="form-group mx-sm-3 mb-2">
        <%= label_tag :password, 'Contraseña', class:'' %>
        <%= password_field_tag :password, params[:password], class:"form-control" %>
      </div>
      <%= submit_tag "Loguearse", class:'btn btn-danger mb-2'%>
      </div>

  <%end %>
  </div>
  <div class="col-12 col-md-2">
  </div>
  </div>
<% end %>
