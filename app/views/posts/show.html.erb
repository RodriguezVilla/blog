<% if notice %><p id="notice"><%= notice %></p><% end %>
<h2 class="pt-4"><%= @post.titulo %></h2>
<p class="text-center">  <%= image_tag(@post.img_destacada_url) %></p>
<div class="container">
<%= (@post.descripticion).html_safe %>
</div>

<p class="pt-3"> <strong>Fecha:</strong> <%= @post.fecha %> </p>
<p class="h4"> añade un comentario </p>
<%= form_with(model: comment, local: true) do |form| %> 
  <div class="field">
    <%= form.label :commeter %>
    <%= form.text_field :commeter %>
  </div>
  <div class="field">
    <%= form.label :text_comment %>
    <%= form.text_area :text_comment %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
  <% end %>
<% if (@usu.call) %>
<% if (@total.call || @blog.call)   %>
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
<% end %>
<% end %>
